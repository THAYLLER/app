import React from 'react';
import {
  Text,
  View,
  TouchableOpacity,
  Animated,
} from 'react-native';

export const DoneButton = ({
  styles, onDoneBtnClick, onNextBtnClick,
  rightTextColor, isDoneBtnShow,
  doneBtnLabel, nextBtnLabel,
  doneFadeOpacity, skipFadeOpacity, nextOpacity,
}) => (
  <View style={[styles.btnContainer, { marginRight: 30 }]}>
    <TouchableOpacity
      style={styles.full}
      onPress={isDoneBtnShow ? onDoneBtnClick : onNextBtnClick}
      hitSlop={{
        top: 20, bottom: 20, left: 80, right: 50,
      }}
    >
      <Animated.View style={[styles.full, { height: 0 }, { opacity: 1 }]}>
        <Text style={[styles.nextButtonText, { color: rightTextColor }]}>
          {isDoneBtnShow ? doneBtnLabel : nextBtnLabel}
        </Text>
      </Animated.View>
    </TouchableOpacity>
  </View>
);

export default DoneButton;
